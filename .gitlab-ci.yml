image: alpine:latest

pages:
  cache:
    paths:
      - node_modules/
  stage: deploy
  script:
  - npm install -g @angular/cli
  - npm install
  - npm build --prod
  - mv dist/futurice* public.
  artifacts:
    paths:
    - public
  only:
  - master
  - pages
